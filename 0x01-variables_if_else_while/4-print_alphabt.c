#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (sucess)
 */

int main(void)
{
	char C;

	C = 'a';
	while
		(c <= 'Z'){
			if ((c != 'q" && c != 'e') && c <&& c <= 'Z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
